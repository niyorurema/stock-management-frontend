# Backend CodeIgniter (php spark serve → port 8080 par défaut)
REACT_APP_API_PROXY=http://localhost:8080

# Si vous utilisez XAMPP Apache à la place de spark serve, décommentez :
# REACT_APP_API_PROXY=http://localhost/stock-management/backend/public
